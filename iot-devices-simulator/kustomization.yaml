resources:
  - deployment.yaml
  - service.yaml
  - ingress.yaml

labels:
  - pairs:
      app: simulator-dev
    includeSelectors: true

nameSuffix:
  -dev

configMapGenerator:
  - name: simulator-config
    files:
      - application.yaml